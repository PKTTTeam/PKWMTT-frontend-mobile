import { StyleSheet } from 'react-native';

const GroupSelectStyles = StyleSheet.create({
  text: {
    color: 'white',
    fontSize: 16,
    marginBottom: 5,
  },
  menuContainer: {
    alignItems: 'center',
  },
});

export default GroupSelectStyles;
