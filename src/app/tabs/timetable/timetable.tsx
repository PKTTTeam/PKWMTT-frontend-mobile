import {
  Text,
  View,
  FlatList,
  ListRenderItemInfo,
  RefreshControl,
  TouchableOpacity,
} from 'react-native';
const Timetable = () => {
  return (
    <View
      style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}
    ></View>
  );
};

export default Timetable;
